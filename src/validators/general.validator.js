module.exports = {
    // Confirms that all required parameters are present and are valid ints
    allPresentIntValidator : (requiredParams, paramsToCheck) => {
        var result = {
            isValid: true,
            message: ''
        };

        // TODO: Finish implementation

        return result;
    }
}